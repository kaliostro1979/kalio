/*
import {SUBMIT_REGISTER} from "../types";

const register = ()=>{
    return async (dispatch)=>{
        const sendRegisterInfo = await fetch('http://localhost:5000/register',{
            method: 'POST',
            mode: 'cors',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify(data)
        })
    }
}


const submitRegister = ()=>{
    return {
        type: SUBMIT_REGISTER
    }
}*/
